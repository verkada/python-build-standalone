# Describes the runners that the CI system can use

ubuntu-latest:
  arch: x86_64
  platform: linux
  free: true

ubuntu-24.04:
  arch: x86_64
  platform: linux
  free: true

ubuntu-22.04:
  arch: x86_64
  platform: linux
  free: true

ubuntu-latest-aarch64:
  arch: aarch64
  platform: linux
  free: true

macos-latest:
  arch: x86_64
  platform: darwin
  free: true

macos-14:
  arch: aarch64
  platform: darwin
  free: true

macos-13:
  arch: x86_64
  platform: darwin
  free: true

windows-latest:
  arch: x86_64
  platform: windows
  free: true

windows-2022:
  arch: x86_64
  platform: windows
  free: true
